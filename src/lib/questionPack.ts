export type QuestionPack = {
    id: string;
    name: string;
    isPublic: boolean;
    keywords: string[];
    questions: string[];
}

export type QuestionPackListItem = {
    id: string;
    name: string;
    isPublic: boolean;
    keywords: string[];
}